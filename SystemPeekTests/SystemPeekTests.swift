import XCTest
@testable import SystemPeek

/// Placeholder so the unit-test target builds from the first commit.
/// Real metric tests arrive with the metrics feature.
final class SystemPeekTests: XCTestCase {
    func testTargetBuilds() {
        XCTAssertTrue(true)
    }
}
